// Calendar tab (REQUIREMENTS.md §2.2): daily / weekly / monthly views with
// 15-minute slot granularity, drag to position/resize, person filter.
// TODO: view switcher, date navigation, person filter, grid(s), library drawer.
export default function CalendarView(): React.JSX.Element {
  return <div className="p-4">Calendar — not implemented yet</div>;
}