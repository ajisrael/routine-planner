// Task editor: name, duration (15-min), notes, category, assignees,
// recurrence pattern (REQUIREMENTS.md §2.1, §2.3).
// TODO: form fields incl. recurrence builder (weekly_days / interval_days /
// monthly_date / monthly_weekday), save → POST/PUT task + assignees + rule.
export default function TaskForm(): React.JSX.Element {
  return <div className="p-4">Task form — not implemented yet</div>;
}