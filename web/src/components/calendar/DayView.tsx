// Day view: one column, 96 × 15-min slots, events as positioned blocks.
// TODO: minutes→px geometry (shared with week), absolute positioned events,
// dnd-kit move/resize, drop target = 15-min slot.
export default function DayView(): React.JSX.Element {
  return <div className="p-4">Day view — not implemented yet</div>;
}