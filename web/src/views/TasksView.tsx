// Task Library tab (REQUIREMENTS.md §2.1): manage tasks, filter by category,
// drag task cards into the calendar during planning mode.
// TODO: task list + category filter + "Add Task" (opens TaskForm).
export default function TasksView(): React.JSX.Element {
  return <div className="p-4">Tasks (library) — not implemented yet</div>;
}