// User/persona management (REQUIREMENTS.md §3.4, DATA_MODEL.md §3.1).
// Login users (Mom, Dad) vs personas (children) that never log in.
// TODO: list users, create persona (is_login_user=0), edit display name.
export default function UserManager(): React.JSX.Element {
  return <div className="p-4">Users — not implemented yet</div>;
}